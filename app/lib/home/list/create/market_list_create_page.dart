import 'package:flutter/material.dart';

class MarketListCreatePage extends StatelessWidget {
  const MarketListCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text("Nome da lista"),
        ],
      ),
    );
  }
}
